{
  "name": "searching-for-javascript-api",
  "version": "1.0.0",
  "description": "API to search #javascript on twitter",
  "scripts": {
    "start": "nodemon --exec \"yarn run start:once\" -w src -e js",
    "start:once": "yarn run build && node lib/index.js",
    "build": "babel src --out-dir lib --extensions='.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "main": "index.js",
  "author": "Heini Ahokas <ahokas.heini@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.8.4",
    "@babel/preset-env": "^7.8.4",
    "nodemon": "^2.0.2"
  }
}
